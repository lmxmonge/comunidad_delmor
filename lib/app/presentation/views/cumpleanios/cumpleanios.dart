import 'package:flutter/material.dart';

class Cumpleanios extends StatelessWidget {
  const Cumpleanios({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Cumpleaños"),);
  }
}
